targets:
  $default:
    builders:
      json_serializable:
        options:
          # Sérialise récursivement les objets imbriqués (Show → Season →
          # Episode) en Map, requis pour écrire dans Firestore.
          explicit_to_json: true
